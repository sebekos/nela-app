import React from "react";
import styled from "styled-components";

const Container = styled.div`
    padding: 4rem 0 0;
    margin: auto;
`;

const Dashboard = () => {
    return <Container>Welcome to Dashboard</Container>;
};

export default Dashboard;
