1. Clone Repo
2. "touch .env"
3. Add to .env: 
    MYSQL_HOST=host
    MYSQL_USER=user
    MYSQL_PASSWORD=pwd
    MYSQL_DATABASE=db
    jwtSecret=secret
    REGISTER_KEY=key
4. "npm install"
5. "npm run dev"
